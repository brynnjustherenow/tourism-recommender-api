# 快速部署指南 🚀

本指南帮助你在 **5-10 分钟内** 将后端服务部署到云端，用于测试和演示。

## 📋 前置准备

- ✅ GitHub 账号（已推送到 GitHub）
- ✅ 代码已提交到本地 Git
- ✅ 一个浏览器

---

## 🎯 推荐方案：Render.com（完全免费）

### 第一步：注册 Render（1分钟）

1. 访问 https://render.com
2. 点击 "Get Started"
3. 使用 GitHub 登录并授权

### 第二步：创建数据库（2分钟）

1. 在 Render 控制台，点击 **"New +"**
2. 选择 **"PostgreSQL"**
3. 填写配置：
   - **Name**: `tourism-recommender-db`
   - **Database**: `tourism_recommender`
   - **User**: `tourism_user`
   - **Region**: 选择 **Singapore**（国内访问更快）
   - **Plan**: 选择 **Free**
4. 点击 **"Create Database"**
5. 等待 2-3 分钟直到数据库创建完成

### 第三步：获取数据库信息（1分钟）

1. 点击刚创建的数据库
2. 复制以下信息（在 "Connection" 部分）：
   - **Internal Database URL**（最方便，直接用于环境变量）
   - 或者分别复制：Host, Port, User, Password

### 第四步：创建 Web 服务（3分钟）

1. 点击 **"New +"**
2. 选择 **"Web Service"**
3. 选择你的 GitHub 仓库
4. 配置如下：

   **Build & Deploy**
   - **Name**: `tourism-recommender-api`
   - **Region**: 选择与数据库相同的区域
   - **Branch**: `main`
   - **Runtime**: `Go`
   - **Build Command**: `go build -o main main.go`
   - **Start Command**: `./main`

   **Advanced** → **Environment Variables**
   
   添加以下环境变量：

   ```bash
   # 应用配置
   GIN_MODE=release
   SERVER_PORT=8080
   
   # 数据库配置（从 Render 数据库页面复制）
   DB_HOST=d-xxx.singapore-postgres.render.com
   DB_PORT=5432
   DB_USER=tourism_user
   DB_PASSWORD=你的数据库密码
   DB_NAME=tourism_recommender
   DB_SSLMODE=require
   
   # 管理员默认配置
   DEFAULT_ADMIN_USERNAME=admin
   DEFAULT_ADMIN_PASSWORD=你的安全密码
   DEFAULT_ADMIN_EMAIL=admin@tourism.com
   
   # 二维码配置
   BASE_URL=https://你的应用名称.onrender.com
   WX_APP_ID=wxa1802e324f2f4712
   WX_APP_PATH=pages/recommendor/detail
   ```

5. 点击 **"Create Web Service"**

### 第五步：等待部署完成（3-5分钟）

1. 点击服务名称查看部署进度
2. 等待状态变为 **"Live"**
3. 部署完成后，你会看到一个 URL，如：
   ```
   https://tourism-recommender-api.onrender.com
   ```

### 第六步：测试 API（1分钟）

在浏览器或终端中测试：

```bash
# 测试健康检查
curl https://tourism-recommender-api.onrender.com/api/v1/health

# 预期响应：
# {"status":"ok","message":"Tourism Recommender API is running"}

# 测试获取推荐官列表
curl https://tourism-recommender-api.onrender.com/api/recommendors
```

---

## 📱 配置小程序

打开 `wxapp/app.js`，修改 API 地址：

```javascript
App({
  globalData: {
    // 替换为你的 Render URL
    apiBaseUrl: "https://tourism-recommender-api.onrender.com/api",
    // ...
  },
});
```

---

## ⚡ 常见问题快速解决

### ❌ 部署失败

**问题**：Build 失败

**解决**：
1. 检查 `go.mod` 和 `go.sum` 是否已提交
2. 检查代码是否能本地编译：`go build main.go`
3. 查看 Build 日志中的错误信息

### ❌ 启动失败

**问题**：服务启动后立即崩溃

**解决**：
1. 检查 `DB_PASSWORD` 是否已设置（这是必需的）
2. 检查数据库凭证是否正确
3. 查看启动日志

### ❌ 数据库连接失败

**问题**：无法连接到数据库

**解决**：
1. 确认数据库和 Web 服务在同一区域
2. 检查 `DB_SSLMODE` 设置为 `require`
3. 确认数据库密码正确

### ❌ API 无法访问

**问题**：部署成功但 API 返回 404

**解决**：
1. 确认使用正确的路径：`/api/v1/health`（不是 `/api/health`）
2. 等待服务完全启动（首次可能需要 30 秒）
3. 检查服务状态是否为 "Live"

---

## 🎉 完成！

现在你的后端服务已经部署到云端了！

### 接下来可以：

1. ✅ 在小程序中测试所有功能
2. ✅ 生成二维码测试扫码功能
3. ✅ 分享给朋友测试
4. ✅ 查看部署日志监控服务状态

---

## 📚 详细文档

如果需要更详细的信息，请查看：

- **[DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)** - 完整部署指南
- **[README.md](./README.md)** - 部署说明和方案对比
- **[.env.example](./.env.example)** - 环境变量完整列表

---

## 🆘 需要帮助？

遇到问题？

1. 查看 Render 文档：https://render.com/docs
2. 检查部署日志（Logs 标签）
3. 查看 DEPLOYMENT_GUIDE.md 中的常见问题部分
4. 检查你的 GitHub Issues

---

**祝部署顺利！** 🚀