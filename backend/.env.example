# ============================================================================
# 旅游推荐官系统 - 环境变量配置示例
# ============================================================================
# 复制此文件为 .env 并根据你的实际环境修改配置值
# 注意：.env 文件不应提交到版本控制系统（已在 .gitignore 中配置）
# ============================================================================

# ----------------------------------------------------------------------------
# 数据库配置
# ----------------------------------------------------------------------------

# PostgreSQL 数据库主机地址
# 默认值: localhost
DB_HOST=localhost

# PostgreSQL 数据库端口
# 默认值: 5432
DB_PORT=5432

# PostgreSQL 数据库用户名
# 默认值: postgres
DB_USER=postgres

# PostgreSQL 数据库密码（必填）
# 请将 your_password_here 替换为实际的数据库密码
DB_PASSWORD=your_password_here

# PostgreSQL 数据库名称
# 默认值: tourism_recommender
# 首次运行前请确保数据库已创建: CREATE DATABASE tourism_recommender;
DB_NAME=tourism_recommender

# PostgreSQL SSL 模式
# 可选值: disable, require, verify-ca, verify-full
# 本地开发环境通常使用 disable
# 生产环境建议使用 require 或更严格的模式
DB_SSLMODE=disable

# ----------------------------------------------------------------------------
# 服务器配置
# ----------------------------------------------------------------------------

# HTTP 服务器监听端口
# 默认值: 8080
SERVER_PORT=8080

# Gin 框架运行模式
# 可选值: debug, release, test
# - debug: 开发模式，输出详细日志
# - release: 生产模式，只输出关键日志
# - test: 测试模式
GIN_MODE=debug

# ----------------------------------------------------------------------------
# 二维码生成配置
# ----------------------------------------------------------------------------

# 二维码跳转的基础 URL
# 用于生成网页版二维码，扫描后跳转到推荐官的网页详情页
# 示例: http://localhost:8080 或 https://yourdomain.com
BASE_URL=http://localhost:8080

# 微信小程序 AppID
# 用于生成小程序版二维码
# 请在微信公众平台获取你的小程序 AppID
# 示例: wx1234567890abcdef
WX_APP_ID=your_miniprogram_appid

# 微信小程序页面路径前缀
# 小程序详情页的路径前缀
# 默认值: /
WX_APP_PATH=/

# ============================================================================
# 配置说明
# ============================================================================
#
# 1. 首次运行步骤：
#    a. 安装 PostgreSQL 数据库
#    b. 创建数据库: CREATE DATABASE tourism_recommender;
#    c. 复制此文件为 .env: cp .env.example .env
#    d. 修改 .env 中的数据库密码等配置
#    e. 运行程序: go run main.go
#
# 2. 环境变量优先级：
#    - 系统环境变量 > .env 文件 > 默认值
#    - 可以在命令行中覆盖: DB_PASSWORD=mypass go run main.go
#
# 3. 生产环境建议：
#    - 使用强密码
#    - 启用 SSL (DB_SSLMODE=require)
#    - 使用 HTTPS (BASE_URL=https://...)
#    - 设置 GIN_MODE=release
#    - 不要在代码中硬编码敏感信息
#
# 4. 故障排查：
#    - 如果连接失败，检查 PostgreSQL 服务是否运行
#    - 确认数据库密码正确
#    - 验证防火墙设置
#    - 查看日志输出的详细错误信息
#
# ============================================================================
