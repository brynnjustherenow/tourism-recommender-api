# API 基础 URL 配置
#
# 开发环境说明：
# - 如果使用 webpack-dev-server (npm start)，可以使用相对路径 "/api"
# - proxy 配置会自动将 /api 请求转发到后端服务器（默认 http://localhost:8080）
#
# 生产环境说明：
# - 如果前端和后端部署在同一域名下，使用相对路径 "/api"
# - 如果部署在不同域名下，设置完整的后端 URL（例如：http://your-backend.com/api）

# 开发环境配置（默认）
REACT_APP_API_BASE_URL=/api

# 如果后端在不同地址，取消下面这行并修改为实际地址
# REACT_APP_API_BASE_URL=http://localhost:8080/api
